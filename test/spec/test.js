/* global describe, it */

(function () {
    'use strict';

    describe('Give it some context', function () {
        describe('maybe a bit more context here', function () {
            it('should run here few assertions', function () {

            });
        });
    });
})();

// test ideas
// make sure no nodes or edges have 'undefined' as a color
// make sure no nodes or edges have 'undefined' as a size